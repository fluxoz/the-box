[package]
name = "box-core"
version.workspace = true
edition = "2021"
description = "The Box: shared disk probe, storage-layout resolution and disko generation"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11.0"
