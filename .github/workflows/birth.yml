# The nightly birth: a blank VM takes the LIVE thebox.build one-liner and
# becomes a serving Box, timed stage by stage. The receipt it emits is pushed
# to the `receipts` branch, which the landing page reads client-side — so
# "last verified <tonight>" on thebox.build is a fact, not copy. A red run
# here means the funnel is broken for real people, which is exactly what a
# nightly should catch.
name: Nightly birth

on:
  schedule:
    - cron: "14 9 * * *" # nightly, offset from busy on-the-hour load
  workflow_dispatch: {}

permissions:
  contents: write # push the receipt branch

concurrency:
  group: birth
  cancel-in-progress: false

jobs:
  birth:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v7
      - name: Install the theater (qemu + UEFI + cloud-init tooling)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq qemu-system-x86 ovmf genisoimage
      - name: Rehearse the birth against the live site
        run: |
          mkdir -p /tmp/birth
          OUT=/tmp/birth bash scripts/rehearse-birth.sh
          cat /tmp/birth/receipt.json
      - name: Publish the receipt
        if: always()
        run: |
          [ -f /tmp/birth/receipt.json ] || exit 0
          git config user.name "the-box-nightly"
          git config user.email "nightly@thebox.build"
          git checkout --orphan receipts-tmp
          git rm -rf --quiet . || true
          mkdir -p receipts
          cp /tmp/birth/receipt.json receipts/latest.json
          git add receipts/latest.json
          git commit -m "receipt: $(date -u +%Y-%m-%dT%H:%MZ)"
          git push -f origin receipts-tmp:receipts
